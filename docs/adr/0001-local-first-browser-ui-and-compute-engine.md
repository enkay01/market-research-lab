# Use a local-first browser interface and local compute engine

The full product will run on one machine as a TypeScript browser interface backed by a local Python compute process. This preserves a productive web interface while giving the analytics layer access to Python's modelling ecosystem, the filesystem, scheduled work, and local data storage; GitHub Pages and cloud infrastructure are outside the current product boundary.

