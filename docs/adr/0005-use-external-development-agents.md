# Use external development agents for technique implementation

Research summaries and papers will be translated into ordinary product code by an external development agent working in the repository, not by an LLM embedded in the application. This keeps the runtime local and deterministic while allowing the agent to summarize a source, plan the implementation, add code and tests, and hand the inactive result to the Analyst for review.
